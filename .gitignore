# Secrets — ห้าม commit เด็ดขาด
.env

# ฐานข้อมูลแคช — ไม่ควร commit ขึ้น git
cache.db
cache.db-journal
cache.db-wal
cache.db-shm

# ไฟล์ debug ที่ ODDS_DEBUG_DUMP=1 เซฟไว้ (raw response จาก OddsPapi)
debug_raw_odds.json

# Virtual environment
venv/
.venv/

# Python cache
__pycache__/
*.pyc
